# Pool Leaderboard

## Self-hosting

**Requirements**

- Docker Compose

**Setup**

1. Clone the repository
2. Create `players.csv` and `.env` from the given sample files
3. Create and run containers:
```bash
docker-compose up -d --build
```
